# Web server configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production
# TRUST_PROXY is passed directly to Express. Keep 1 when running behind a reverse proxy (default).
TRUST_PROXY=1

# Comma-separated list of allowed origins for Socket.IO. Leave empty to allow same-origin requests.
# CORS_ORIGINS=https://example.com,https://app.example.com

# List of STUN/TURN servers used by WebRTC peers. Must be a JSON array.
ICE_SERVERS=[{"urls":"stun:stun.l.google.com:19302"},{"urls":"stun:stun1.l.google.com:19302"}]
